import styled from "styled-components";
import ReactList from "react-list";

const ColorsContainer = styled.div`
  width: auto;
  display: flex;
  justify-content: center;
`;

const ColorsList = styled(ReactList)``;

export { ColorsContainer, ColorsList };
